// Admin configuration
export const ADMIN_EMAIL = 'wannikid@gmail.com';

// Firestore collection names
export const COLLECTIONS = {
    BOOKINGS: 'bookings',
    USERS: 'users'
};

// UI selectors
export const SELECTORS = {
    CALENDAR: '#calendar',
    LOGIN_FORM: '#loginForm',
    USER_INFO: '#userInfo',
    USER_EMAIL: '#userEmail',
    EMAIL_INPUT: '#email',
    NAME_INPUT: '#name'
};

// Button classes
export const BUTTON_CLASSES = {
    BOOK: 'book',
    CANCEL: 'cancel'
};

// Button text
export const BUTTON_TEXT = {
    BOOK: "I'm coming",
    CANCEL: 'Cancel'
};

// DOM classes
export const DOM_CLASSES = {
    CALENDAR_DAY: 'calendar-day',
    WEEKEND: 'weekend',
    BOOKED_INFO: 'booked-info'
};
